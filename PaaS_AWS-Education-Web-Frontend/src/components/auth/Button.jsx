import Button from '../common/Button';

/**
 * Re-export Button component từ common/Button để đảm bảo tương thích ngược
 * với các component đang sử dụng Button từ auth folder
 */
export default Button;
